export const metadata = {
  title: "Projects",
  description: "Explore our portfolio of luxury residential, commercial, and renovation projects.",
};

export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
