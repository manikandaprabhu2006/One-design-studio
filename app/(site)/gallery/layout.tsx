export const metadata = {
  title: "Gallery",
  description: "Explore our gallery of luxury architecture and interior design projects.",
};

export default function GalleryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
